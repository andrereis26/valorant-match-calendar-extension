{
  "name": "valorant-match-calendar",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/popup.ts src/options.ts src/background.ts --bundle --outdir=dist --format=iife --target=chrome120",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.327",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
